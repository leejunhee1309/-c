#include <stdio.h>

int main(void)
{
	int A, B;
	
	scanf("%d %d", &A, &B);
	
	if (B - 45 < 0) {
		A = A - 1;
		B = 60 + B;
		
		if (A < 0) {
			A = A + 24;
		}
	}
	
	if (B - 45 >= 0); {
		B = B - 45;

	}

	printf("%d %d", A, B);

	return 0;
}
